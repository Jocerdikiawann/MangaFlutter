class MangaModel {
  String? title, thumb, type, updateOn, endpoint, chapter;
  MangaModel(
      {this.title,
      this.thumb,
      this.type,
      this.updateOn,
      this.endpoint,
      this.chapter});
}
