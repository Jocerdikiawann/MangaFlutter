class PopularModel {
  String? title, thumb, type, uploadOn, endpoint, chapter;
  PopularModel(
      {this.title,
      this.thumb,
      this.type,
      this.uploadOn,
      this.endpoint,
      this.chapter});
}
